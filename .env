# Local development database. Points at the docker-compose Postgres (see docker-compose.yml).
# Non-secret local credentials — safe to commit. On Vercel, set DATABASE_URL in the
# project's Environment Variables (the Postgres integration provides it) and it overrides this.
DATABASE_URL="postgresql://relay:relay@localhost:5432/relay?schema=public"
